@{
Layout= "_Layout";
}
@using HairSalon.Models

<div class ="container">
  <h1>Employees</h1>
  @if (@Model.Count == 0)
  {
  <h3>There are no employees listed at this location</h3>
  <p>Add a add new employee button here Chris</p>
  }

  @foreach (Stylist stylist in Model)
  {
  <h6><a href ='/stylist/@stylist.GetId()'>@stylist.GetName()</a></h6>
  }

// should link back to homepage

  <button btn-dark type="submit" action="/">Back home</button>
</div>
